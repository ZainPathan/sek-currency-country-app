import { makeStyles } from '@material-ui/core/styles';

const useInputTextButtonComboStyles = makeStyles({
    inputField: {
        height: 20,
        width: '100%',
    },
    button: {
        height: 60,
        width: '100%',
        marginTop: 60
    }
});

export default useInputTextButtonComboStyles;